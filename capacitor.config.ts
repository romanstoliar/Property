import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'wproperty.webart.work',
	appName: 'wProperty',
	webDir: 'dist/app/browser'
};

export default config;
